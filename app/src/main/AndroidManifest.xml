<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scsa.android.selfManagement" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarrotCake" >
        <activity
            android:name=".helloWord.HelloWordActivityStartView"
            android:exported="false" />
        <activity
            android:name=".helloWord.HelloWordActivityView"
            android:exported="false" />
        <activity
            android:name=".newFeed.NewFeedViewActivity"
            android:exported="false" />
        <activity
            android:name=".toDoList.ToDoDetailsActivityView"
            android:exported="true" />
        <activity
            android:name=".toDoList.ToDoListActivityView"
            android:exported="true" />
        <activity
            android:name=".MainActivityView"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>